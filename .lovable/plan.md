

## Fix Countdown Number Text Shadow

### Single edit in `src/index.css`
Replace the 7-layer `text-shadow` in `.countdown-number` with the requested 3-layer pink glow. Keep `color: #ffffff`. No other changes.

