

## Switch Audio from WAV to MP3

1. Copy uploaded `mixkit-long-pop-2358.mp3` to `public/sounds/mixkit-long-pop-2358.mp3`
2. In `src/pages/Index.tsx`, replace both occurrences of `'/sounds/mixkit-long-pop-2358.wav'` with `'/sounds/mixkit-long-pop-2358.mp3'`

No other changes.

